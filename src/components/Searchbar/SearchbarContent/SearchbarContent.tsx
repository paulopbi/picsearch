import "./SearchbarContent.css";

const SearchbarContent = ({ children }: { children: React.ReactNode }) => {
	return <div className="search-content">{children}</div>;
};

export default SearchbarContent;
